# Core
torch>=2.2,<3.0
numpy>=2.0,<3.0
scipy>=1.11,<2.0
einops>=0.7,<1.0
tqdm>=4.66,<5.0
omegaconf>=2.3,<3.0
pyyaml>=6.0,<7.0
imageio>=2.31,<3.0
imageio-ffmpeg>=0.4,<1.0

# Vision
opencv-python-headless>=4.8,<5.0
kornia>=0.7,<1.0
Pillow>=10.0,<12.0
timm>=0.9,<2.0

# 3D
plyfile>=1.0,<2.0

# ML tracking
wandb>=0.16,<1.0
tensorboard>=2.14,<3.0

# Depth estimation
transformers>=4.36,<6.0
safetensors>=0.4,<1.0

# NOTE: On Linux with CUDA, also install:
#   pip install gsplat>=1.0 open3d>=0.17
#
# PyTorch requires platform-specific installation.
# See scripts/setup_env.sh or use:
#   macOS:       pip install torch torchvision torchaudio
#   Linux CUDA:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   Linux CPU:   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
